__pycache__/
*.py[cod]
.venv/
.venv-ocr.nosync/
venv/
.pytest_cache/
*.egg-info/
dist/
build/
.DS_Store
.env
.env.local
.local/
models/
benchmarks/handwriting/*.png
.playwright-cli/
output/playwright/

# spice / simulation artifacts
*.raw
*.out
*.log

# subagent-driven-development scratch (ledger, briefs, review packages)
.superpowers/

# linkC multi-agent coordination state
.linkc/

# Swift package build output for the macOS app: 250 MB of module cache that
# swift build regenerates.
macos/.build/
macos/.swiftpm/

# A student's own line crops for the handwriting eval; the manifest that names
# them is committed, the coursework images are not.
benchmarks/handwriting/crops/

# /brag launch-video workspace; only the README GIF (docs/media/) is tracked
brag/
